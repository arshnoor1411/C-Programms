#include<stdio.h>
int main()
{
	int num,count=0,x,size,i;
	printf("Enter Number:");
	scanf("%d",&num);
	while(num!=0)
	{
		num=num/10;
		count++;
	} 
	count=size;
	int arr[num];
	for(i=0;i<size;i++)
	{
		x=arr[i];
}
printf("%d",x);
	return 0;
}
